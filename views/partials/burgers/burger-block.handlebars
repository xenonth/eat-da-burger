
<li>
	{{burger_name}}

	<button class="devoured" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}
            CONSUME AGAIN?
        {{else}}
            CONSUME
        {{/if}}
	</button>
</li>